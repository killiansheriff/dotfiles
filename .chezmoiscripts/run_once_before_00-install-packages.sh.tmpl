#!/bin/bash
# ╔══════════════════════════════════════════════════════════════════════════╗
# ║                    CHEZMOI BOOTSTRAP SCRIPT                              ║
# ║          Runs once before applying dotfiles on a new machine             ║
# ╚══════════════════════════════════════════════════════════════════════════╝

# Don't exit on error - just warn
set +e

echo "╔══════════════════════════════════════════════════════════════════╗"
echo "║           Setting up {{ .chezmoi.hostname }}                     "
echo "╚══════════════════════════════════════════════════════════════════╝"
echo ""

{{ if eq .chezmoi.os "darwin" -}}
# ─────────────────────────────────────────────────────────────────────────────
# macOS: Install Homebrew if not present
# ─────────────────────────────────────────────────────────────────────────────
if ! command -v brew &> /dev/null; then
    echo "📦 Installing Homebrew..."
    /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)" || echo "  ⚠️  Warning: Failed to install Homebrew"
    eval "$(/opt/homebrew/bin/brew shellenv)" 2>/dev/null
fi

if command -v brew &> /dev/null; then
    echo "📦 Installing packages via Homebrew..."
    brew install --quiet \
        git \
        eza \
        bat \
        fzf \
        zoxide \
        fd \
        ripgrep \
        coreutils \
        2>/dev/null || echo "  ⚠️  Warning: Some Homebrew packages failed to install"
fi

{{ else if eq .chezmoi.os "linux" -}}
# ─────────────────────────────────────────────────────────────────────────────
# Linux: Try to install packages (will warn if no sudo access)
# ─────────────────────────────────────────────────────────────────────────────
if command -v apt-get &> /dev/null; then
    echo "📦 Attempting to install packages via apt..."
    if sudo -n true 2>/dev/null; then
        sudo apt-get update -qq 2>/dev/null
        sudo apt-get install -y -qq git curl fzf ripgrep fd-find bat 2>/dev/null || echo "  ⚠️  Warning: Some apt packages failed to install"
    else
        echo "  ⚠️  Warning: No sudo access - skipping apt packages"
        echo "  📝 Install manually: see README.md for instructions"
    fi
elif command -v dnf &> /dev/null; then
    echo "📦 Attempting to install packages via dnf..."
    if sudo -n true 2>/dev/null; then
        sudo dnf install -y -q git curl fzf ripgrep fd-find bat 2>/dev/null || echo "  ⚠️  Warning: Some dnf packages failed to install"
    else
        echo "  ⚠️  Warning: No sudo access - skipping dnf packages"
        echo "  📝 Install manually: see README.md for instructions"
    fi
fi

{{ end -}}

# ─────────────────────────────────────────────────────────────────────────────
# Install zsh plugins (no sudo required)
# ─────────────────────────────────────────────────────────────────────────────
echo "📦 Installing zsh plugins..."
mkdir -p ~/.zsh

if [[ ! -d ~/.zsh/zsh-autosuggestions ]]; then
    git clone --depth=1 https://github.com/zsh-users/zsh-autosuggestions ~/.zsh/zsh-autosuggestions 2>/dev/null || echo "  ⚠️  Warning: Failed to clone zsh-autosuggestions"
fi

if [[ ! -d ~/.zsh/zsh-syntax-highlighting ]]; then
    git clone --depth=1 https://github.com/zsh-users/zsh-syntax-highlighting ~/.zsh/zsh-syntax-highlighting 2>/dev/null || echo "  ⚠️  Warning: Failed to clone zsh-syntax-highlighting"
fi

# ─────────────────────────────────────────────────────────────────────────────
# Create necessary directories
# ─────────────────────────────────────────────────────────────────────────────
echo "📁 Creating directories..."
mkdir -p ~/.ssh/sockets
mkdir -p ~/.ssh/config.d
mkdir -p ~/.local/bin
chmod 700 ~/.ssh 2>/dev/null
chmod 700 ~/.ssh/sockets 2>/dev/null

echo ""
echo "✅ Bootstrap complete!"
echo ""
